# ── Unity generated ───────────────────────────────────────────────────────────
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Uu]ser[Ss]ettings/
*.csproj
*.sln
*.suo
*.user
*.pidb
*.pdb
*.mdb
*.opendb

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Python backend ────────────────────────────────────────────────────────────
backend/__pycache__/
backend/*.pyc
backend/.env
backend/venv/
backend/.venv/

# ── API config — edit URL in Unity Inspector, never commit it ─────────────────
# To switch URLs: open Unity → Assets/Resources/GameSettings.asset → apiBaseUrl
# Fresh clone falls back to http://localhost:8000 automatically.
UnityClient/Assets/Resources/GameSettings.asset
UnityClient/Assets/Resources/GameSettings.asset.meta

# ── WebGL build — hosted separately on gh-pages branch via GitHub Pages ────────
WebGL/

# ── Local Claude Code config (contains tokens) ────────────────────────────────
.claude/
.mcp.json
